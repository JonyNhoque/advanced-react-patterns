import styled from "styled-components";

const S = {

    SearchBar__container: styled.div`
        display: flex;
        align-items: center;
        justify-content: flex-end;
    `,

    Input: styled.input`
        border-radius: 20px;
        max-height: 30px;
    `
};

export default S;